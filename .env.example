# ── Local development (Docker) ──────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=smartreceipt
DB_PASSWORD=smartreceipt123
DB_NAME=smartreceipt

# ── Production (Neon) — replaces individual DB vars ─────
# DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/smartreceipt?sslmode=require

# ── AI ───────────────────────────────────────────────────
GROQ_API_KEY=          # console.groq.com — free, no CC
GEMINI_API_KEY=        # optional, unused

# ── Server ───────────────────────────────────────────────
PORT=3001
UPLOAD_DIR=./uploads
NODE_ENV=development

# ── CORS (production) ────────────────────────────────────
# CORS_ORIGIN=https://your-app.vercel.app
